name: Build and test pull request

on:
  pull_request:
    branches:
      - main

jobs:
  build:
    name: build pull request
    permissions:
      contents: read
      packages: read
    uses: ./.github/workflows/build.yml
